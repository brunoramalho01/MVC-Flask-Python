#aqui onde será criado a estrutura do banco de dados da aplicação# Aqui é onde serão tratados os formulários da aplicação, utilizando Flask-WTF ou outro módulo de formulários

# Exemplo de importação de FlaskForm, que será utilizada na criação de formulários personalizados
# from flask_wtf import FlaskForm
# from wtforms import StringField, SubmitField
# from wtforms.validators import DataRequired

# class ExampleForm(FlaskForm):
#     name = StringField('Name', validators=[DataRequired()])
#     submit = SubmitField('Submit')
